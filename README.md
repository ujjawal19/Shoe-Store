I have dockerize the application in order to run it you just need to run the following command:
### `docker compose up`


Frontend will be running on: `http://localhost:3000`

and backend : `http://localhost:3004`